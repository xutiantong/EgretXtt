export class ShakeUtils {    static generateShakeRadii() {        for (var e = [], t = 0; t < 14; t++) {            e.push(2 * Math.PI * Math.random());        }        return e    }    //view 1e3 1.5 radii    public static shakeView(e: cc.Node, cb?: () => void) {        var t = t || 1;        var i = i || 1.5;        var n = ShakeUtils.generateShakeRadii();        var time = t / 16, s = e.x, l = e.y, c = e.scale;        var actions = [];        {            var scale = c * (1 + .02 * i);            actions.push(cc.scaleTo(time, scale, scale))        }        var scales = [.046, .042, .038, .034, .03, .026, .022, .018, .014, .01, .008, .006, .004, .002];        for (var ii = 0; ii < 14; ii++) {            var x = s + (14 - ii) * i * Math.cos(n[ii]);            var y = l + (14 - ii) * i * Math.sin(n[ii]);            var scale = c * (1 + scales[ii] * i);            actions.push(cc.spawn(                cc.moveTo(time, x, y).easing(cc.easeIn(3)),                cc.scaleTo(time, scale, scale).easing(cc.easeIn(3))            ))        }        {            actions.push(cc.spawn(                cc.moveTo(time, s, l).easing(cc.easeIn(3)),                cc.scaleTo(time, c, c).easing(cc.easeIn(3))            ))        }        if (cb) {            actions.push(cc.callFunc(cb))        }        e.runAction(cc.sequence(actions))    };}